info:
  name: Edit
  type: http
  seq: 4

http:
  method: PUT
  url: "{{PATH_URL}}/bus"
  body:
    type: json
    data: ""
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
