info:
  name: Edit
  type: http
  seq: 4

http:
  method: PUT
  url: "{{PATH_URL}}/routes/"
  body:
    type: json
    data: |-
      {
        "BusId" : "",
        "Date" : "",
        "Hour" : "",
        "TotalPassengers" : ""
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
