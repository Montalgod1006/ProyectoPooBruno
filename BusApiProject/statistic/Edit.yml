info:
  name: Edit
  type: http
  seq: 6

http:
  method: PUT
  url: "{{PATH_URL}}/statistic"
  body:
    type: json
    data: ""
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
